doctype html
html
  head
    title= `${title} | ${h.siteName}`
    link(rel='stylesheet', href='/dist/style.css')
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body
    block header
      h1 header

    block messages
      h1 flash messages go here

    block content

    block scripts
      script(src="/dist/App.bundle.js")
